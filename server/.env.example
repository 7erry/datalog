# Copy to .env (never commit .env). Rotate credentials if they were ever shared in plain text.
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER_HOST/
DB_NAME=datalog
PORT=3000
# Absolute or relative path from server/ to repo root (parent of *.discovery.md)
REPO_ROOT=..
